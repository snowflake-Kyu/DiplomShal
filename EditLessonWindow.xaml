<Window x:Class="WPFPPShall.EditLessonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFPPShall"
        mc:Ignorable="d"
        Title="EditLessonWindow" Height="272" Width="722">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Изменение урока"
                   FontFamily="Segoe UI"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Время:"
                   Margin="0,0,8,0"
                   VerticalAlignment="Center"/>
        <ComboBox x:Name="cbTimeSlot"
                  Grid.Row="1" Grid.Column="1"
                  Height="24" />

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Предмет:"
                   Margin="0,8,8,0"
                   VerticalAlignment="Top"/>
        <ComboBox x:Name="cbDiscipline"
                  Grid.Row="2" Grid.Column="1"
                  Margin="0,5,0,116"
                  Height="24"/>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2"
                   Text="Учитель и аудитория будут выбраны автоматически по предмету."
                   Margin="0,8,0,8"
                   FontSize="11"
                   Foreground="Gray"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Отмена" Width="90" Margin="0,0,8,0"
                    IsCancel="True" Click="BtnCancel_Click"/>
            <Button Content="Сохранить" Width="90"
                    IsDefault="True" Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>
